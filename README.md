# version-drift
CLI tool to quantify and visualize how a codebase evolved across versions. Scans versioned snapshot directories, tracks lines of code, file growth, and SQL schema changes, generates charts and CSVs, and uses Claude AI to write plain-English narratives of what changed between releases.
